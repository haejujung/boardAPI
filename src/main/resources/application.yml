jwt:
  secret: mysecretkey123456789
spring:
  security:
    oauth2:
      client:
        registration:
          # 1. Google 설정 (표준 Provider)
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: profile,email # 요청할 권한 범위

          # 2. Naver 설정 (비표준 Provider: 별도 설정 필요)
          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            client-authentication-method: post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: name,email,profile_image
            client-name: Naver

          # 3. Kakao 설정 (비표준 Provider: 별도 설정 필요)
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: profile_nickname,account_email # 카카오는 'scope' 대신 'property'를 사용하는 경우도 있습니다.

        provider:
          # Naver Provider 설정
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response # 네이버 사용자 정보 JSON 구조의 최상위 키

          # Kakao Provider 설정
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id # 카카오 사용자 정보의 고유 ID 키
  devtools:
    restart:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: root
    password: asd123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
